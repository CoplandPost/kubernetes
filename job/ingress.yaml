apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-svc
  namespace: application
spec:
  rules:
  - host: app.coplandpost.com/job
    http:
      paths:
      - path: /api-docs
        pathType: Prefix
        backend:
          service:
            name: job-clusterip-svc
            port: 
              number: 5091
  ingressClassName: nginx